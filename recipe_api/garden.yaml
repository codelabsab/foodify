kind: Module
name: recipe-api
type: container
services:
  - name: recipe-api
    ingresses:
      - path: /
        port: http
    env:
      ADDRESS: 0.0.0.0:3000
    dependencies:
      - firestore-emulator
    ports:
      - name: http
        containerPort: 3000